# ACC OS X Build 214 R6.2

## Branding polish
- Neon X remains the official ACC OS X identity.
- New R6.2 versioned PWA icon assets.
- Maskable icon uses a cleaner full dark background and a tighter Android safe-zone to reduce irregular halo/cropping.
- Header logo follows the R6.2 icon asset.

## Mobile navigation
Primary labels on mobile are now:

ENT | CH | PROD | ECO | SYSTEM

Only the display labels changed. Navigation routes and system logic are unchanged.

## Deployment
Upload every file in this patch to the root of the `ACC-OS-X` GitHub repository on branch `main`.
Cloudflare auto-deploys after commit.
