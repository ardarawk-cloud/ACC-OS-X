name: AM DIGITAL LAB Public Website Deploy

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/amdl-public-cloudflare-bridge.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: amdl-public-cloudflare-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout AM DIGITAL LAB monorepo
        uses: actions/checkout@v6
        with:
          repository: ardarawk-cloud/am-digital-lab-crm
          ref: main

      - name: Generate real project portfolio captures
        run: |
          bash scripts/capture-portfolio.sh

      - name: Validate public website
        run: |
          node --check worker-public.js
          node --check site/app.js
          grep -q 'WE BUILD DIGITAL PRODUCTS' site/index.html
          grep -q 'Start a Project' site/index.html
          grep -q 'REAL PROJECT PREVIEW' site/upgrade.css
          grep -q 'sendDiscordLeadNotification' worker-public.js
          grep -q 'sendWhatsAppLeadNotification' worker-public.js
          grep -q "const SITE_VERSION='1.3.0'" worker-public.js
          grep -q 'Sitemap:' site/robots.txt
          grep -q '<urlset' site/sitemap.xml
          for f in acc-os-x amdl-crm zuzu-family-house am-studio-music oracly ai-mashup; do
            test -s "site/portfolio/$f.png"
          done

      - name: Deploy public website
        id: deploy
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4.115.0'
          command: deploy -c wrangler-public.jsonc

      - name: Verify website, lead notifications, real previews and CRM lead bridge
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment-url }}
        run: |
          set -euo pipefail
          echo "Deployment URL: ${DEPLOYMENT_URL:-<empty>}"
          [ -n "${DEPLOYMENT_URL:-}" ] || exit 1
          for attempt in 1 2 3 4 5 6; do
            HEALTH="$(curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/healthz" || true)"
            HOME="$(curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/" || true)"
            UPGRADE="$(curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/upgrade.css" || true)"
            ROBOTS="$(curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/robots.txt" || true)"
            if printf '%s' "$HEALTH" | grep -q 'am-digital-lab-public' && printf '%s' "$HOME" | grep -q 'WE BUILD DIGITAL PRODUCTS' && printf '%s' "$UPGRADE" | grep -q 'REAL PROJECT PREVIEW' && printf '%s' "$ROBOTS" | grep -q 'Sitemap:'; then
              break
            fi
            sleep 5
          done
          echo "Health: $HEALTH"
          printf '%s' "$HEALTH" | grep -q '"ok":true'
          printf '%s' "$HEALTH" | grep -q '"version":"1.3.0"'
          printf '%s' "$HEALTH" | grep -q '"discord_notification_configured":'
          printf '%s' "$HEALTH" | grep -q '"whatsapp_notification_configured":true'
          printf '%s' "$HEALTH" | grep -q '"notification_primary":'
          printf '%s' "$HOME" | grep -q 'AM DIGITAL LAB'
          printf '%s' "$UPGRADE" | grep -q 'REAL PROJECT PREVIEW'
          printf '%s' "$ROBOTS" | grep -q 'Sitemap:'
          curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/portfolio/acc-os-x.png" -o /tmp/acc-os-x.png
          curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/portfolio/oracly.png" -o /tmp/oracly.png
          test "$(wc -c < /tmp/acc-os-x.png)" -gt 10000
          test "$(wc -c < /tmp/oracly.png)" -gt 10000
          CODE="$(curl --silent --show-error --output /tmp/amdl-public-form.txt --write-out '%{http_code}' --max-time 30 -X POST "${DEPLOYMENT_URL%/}/api/start-project" -H 'content-type: application/json' --data '{"name":""}')"
          cat /tmp/amdl-public-form.txt
          echo "Malformed lead bridge status: $CODE"
          [ "$CODE" = "400" ]

      - name: One-time WhatsApp E2E lead test
        if: contains(github.event.head_commit.message, '[wa-e2e-test]')
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment-url }}
        run: |
          set -euo pipefail
          TEST_CODE="$(curl --silent --show-error --output /tmp/amdl-wa-test.txt --write-out '%{http_code}' --max-time 30 -X POST "${DEPLOYMENT_URL%/}/api/start-project" -H 'content-type: application/json' --data '{"name":"AMDL Notification Test","company":"AM DIGITAL LAB Internal Test","email":"notification-test@example.com","project_type":"WhatsApp Notification Test","budget_range":"TEST ONLY","target_launch":"2026-09-14","project_description":"Automated end-to-end WhatsApp notification test. Safe to archive after verification."}')"
          cat /tmp/amdl-wa-test.txt
          echo "WhatsApp E2E lead status: $TEST_CODE"
          [ "$TEST_CODE" = "201" ]

      - name: One-time Discord E2E lead test
        if: contains(github.event.head_commit.message, '[discord-e2e-test]')
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment-url }}
        run: |
          set -euo pipefail
          HEALTH="$(curl --fail --silent --show-error --max-time 30 "${DEPLOYMENT_URL%/}/healthz")"
          printf '%s' "$HEALTH" | grep -q '"discord_notification_configured":true'
          TEST_CODE="$(curl --silent --show-error --output /tmp/amdl-discord-test.txt --write-out '%{http_code}' --max-time 30 -X POST "${DEPLOYMENT_URL%/}/api/start-project" -H 'content-type: application/json' --data '{"name":"AMDL Discord Test","company":"AM DIGITAL LAB Internal Test","email":"notification-test@example.com","project_type":"Discord Notification Test","budget_range":"TEST ONLY","target_launch":"2026-09-14","project_description":"Automated end-to-end Discord notification test. Safe to archive after verification."}')"
          cat /tmp/amdl-discord-test.txt
          echo "Discord E2E lead status: $TEST_CODE"
          [ "$TEST_CODE" = "201" ]

# redeploy marker: Discord-first lead notifications 2026-09-14
