# ACC OS X — Build 214 R6

## Neon X Identity

- Replaces the old checkmark shield in the in-app header with the new ACC OS X Neon X Shield.
- Replaces PWA icons at 192 px, 512 px, and maskable 512 px.
- Header and PWA identity now share the same icon source so future brand swaps are simpler.
- Bumps the service-worker cache to R6 so installed PWAs receive the new visual identity.
- Preserves Build 214 AI, registry, Vault, Queue, Pipeline, and Cloudflare Workers AI behavior.
