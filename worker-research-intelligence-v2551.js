// ACC OS X — BUILD 255.1 KAI RESEARCH INTELLIGENCE
// Resilient multi-source acquisition: official newsroom hubs + targeted news feed +
// Browser Markdown networkidle2 + direct-fetch fallback.
// Publishing / Meta / caption / poster renderer paths are untouched.

import baseWorker from "./worker-stage-normalizer.js";

const REVISION="BUILD255_1_MULTISOURCE_RESEARCH";
const DIRECTOR_MODEL="@cf/qwen/qwen3-30b-a3b-fp8";
const CRITIC_MODEL="@cf/zai-org/glm-4.7-flash";
const MIN_RESEARCH=8.5;
const text=v=>typeof v==="string"?v.trim():"";
const TECH_HUBS=[
  "https://openai.com/news/",
  "https://blog.google/technology/",
  "https://blogs.microsoft.com/",
  "https://blogs.nvidia.com/",
  "https://blog.cloudflare.com/",
  "https://www.apple.com/newsroom/",
  "https://engineering.fb.com/"
];

function json(data,status=200,headersLike=null){const h=new Headers(headersLike||{});h.set("Content-Type","application/json;charset=UTF-8");h.set("Cache-Control","no-store");h.set("Access-Control-Allow-Origin","*");return new Response(JSON.stringify(data,null,2),{status,headers:h});}
function stageOf(body){const s=String(body?.context?.workerTask?.stage||"").toUpperCase();if(s==="MATERIAL")return"SCRIPT";if(s==="PUBLISH")return"PUBLISHING";if(["RESEARCH","SCRIPT","POSTER","CAPTION","QC","PUBLISHING"].includes(s))return s;const joined=(Array.isArray(body?.messages)?body.messages:[]).map(m=>text(m?.content)).join("\n");const m=joined.match(/(?:^|\n)STAGE:\s*(RESEARCH|SCRIPT|POSTER|CAPTION|QC|PUBLISHING)\b/i);return m?String(m[1]).toUpperCase():"";}
function profileOf(body){const p=body?.context?.profile||{};return{id:p.id||"",name:p.name||"",category:p.category||"",platform:p.platform||"",mission:p.mission||"",canon:p.canon||"",kind:p.kind||""};}
function isTechverse(p){return p.id==="ch-techverse"||/techverse/i.test(p.name);}
function today(){return new Date().toISOString().slice(0,10);}
function modelText(r){const xs=[r?.response,r?.result?.response,r?.text,r?.result?.text,r?.output_text,r?.result?.output_text,r?.choices?.[0]?.message?.content,r?.result?.choices?.[0]?.message?.content];for(const v of xs){if(text(v))return text(v).replace(/<think>[\s\S]*?<\/think>/gi,"").replace(/^```(?:text)?\s*|\s*```$/gi,"").trim();}return typeof r==="string"?text(r):"";}
async function runOnce(env,model,system,user,max_tokens=2200,temperature=.25){const r=await env.AI.run(model,{messages:[{role:"system",content:system},{role:"user",content:user}],max_tokens,temperature});const out=modelText(r);if(!out)throw new Error(`${model} returned empty output`);return out;}
async function resilient(env,primary,fallback,system,user,max_tokens=2200,temperature=.25){let err;for(let i=0;i<2;i++){try{return await runOnce(env,primary,system,user,max_tokens,temperature);}catch(e){err=e;}}if(fallback&&fallback!==primary){for(let i=0;i<2;i++){try{return await runOnce(env,fallback,system,user,max_tokens,temperature);}catch(e){err=e;}}}throw err||new Error("AI returned no usable output");}
function safeUrl(raw){try{const u=new URL(String(raw||""));if(!/^https?:$/.test(u.protocol))return"";const h=u.hostname.toLowerCase();if(h==="localhost"||h.endsWith(".local")||/^127\./.test(h)||/^10\./.test(h)||/^192\.168\./.test(h)||/^169\.254\./.test(h))return"";u.hash="";return u.toString().replace(/\/$/,"");}catch{return"";}}
function urlsIn(v){return Array.from(String(v||"").matchAll(/https?:\/\/[^\s)\]}>"']+/g)).map(m=>safeUrl(m[0])).filter(Boolean);}
function uniq(values){const out=[],seen=new Set();for(const x of values||[]){const u=safeUrl(x);if(!u||seen.has(u))continue;seen.add(u);out.push(u);}return out;}
function htmlDecode(s){return String(s||"").replace(/&amp;/g,"&").replace(/&quot;/g,'"').replace(/&#39;|&apos;/g,"'").replace(/&lt;/g,"<").replace(/&gt;/g,">");}
function absLinks(html,base){const out=[];for(const m of String(html||"").matchAll(/href\s*=\s*["']([^"']+)["']/gi)){try{const u=new URL(htmlDecode(m[1]),base).toString();out.push(u);}catch{}}return uniq(out);}
function articleLike(u,hub){try{const a=new URL(u),b=new URL(hub);if(a.hostname!==b.hostname)return false;const p=a.pathname.toLowerCase();if(p==="/"||p.length<8)return false;if(/\/(?:tag|tags|category|categories|author|authors|search|privacy|terms|about|contact|events?)(?:\/|$)/.test(p))return false;if(/\.(?:jpg|jpeg|png|gif|svg|webp|pdf|xml|rss)$/i.test(p))return false;return p.split("/").filter(Boolean).length>=2;}catch{return false;}}
async function quickJson(response){if(!response)return{success:false,result:null};try{const d=await response.json();return{success:Boolean(d?.success),result:d?.result??null};}catch{try{const raw=await response.text();return{success:response.ok,result:raw};}catch{return{success:false,result:null};}}}
async function directFetch(url,timeout=9000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{return await fetch(url,{redirect:"follow",headers:{"User-Agent":"Mozilla/5.0 ACC-OS-X Research Bot","Accept":"text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8"},signal:c.signal});}finally{clearTimeout(t);}}
async function linksFromHub(env,hub){let links=[];try{const r=await directFetch(hub,8000);if(r.ok){const ct=r.headers.get("content-type")||"";if(/html/i.test(ct)){const html=await r.text();links.push(...absLinks(html,hub));}}}catch{}
  if(links.filter(x=>articleLike(x,hub)).length<3&&env?.BROWSER){try{const r=await env.BROWSER.quickAction("links",{url:hub,visibleLinksOnly:false,gotoOptions:{waitUntil:"networkidle2",timeout:12000}});const p=await quickJson(r);if(p.success&&Array.isArray(p.result))links.push(...p.result);}catch{}}
  return uniq(links).filter(x=>articleLike(x,hub)).slice(0,10);
}
function stripHtml(raw){return htmlDecode(String(raw||"").replace(/<script[\s\S]*?<\/script>/gi," ").replace(/<style[\s\S]*?<\/style>/gi," ").replace(/<noscript[\s\S]*?<\/noscript>/gi," ").replace(/<[^>]+>/g," ").replace(/\s+/g," ")).trim();}
async function readPage(env,url){if(env?.BROWSER){try{const r=await env.BROWSER.quickAction("markdown",{url,gotoOptions:{waitUntil:"networkidle2",timeout:14000}});const p=await quickJson(r);const md=text(p.result);if(p.success&&md.length>=300)return{url,content:md.slice(0,7500),mode:"BROWSER_MARKDOWN"};}catch{}}
  try{const r=await directFetch(url,10000);if(r.ok){const ct=r.headers.get("content-type")||"";if(/html|text/i.test(ct)){const raw=await r.text();const plain=stripHtml(raw);if(plain.length>=450)return{url:safeUrl(r.url)||url,content:plain.slice(0,7500),mode:"DIRECT_FETCH"};}}}catch{}
  return null;
}
async function renderMany(env,urls,limit=8){const rows=await Promise.all(uniq(urls).slice(0,16).map(u=>readPage(env,u)));return rows.filter(Boolean).slice(0,limit);}
function bundle(rows){return rows.map((e,i)=>`SOURCE ${i+1}\nURL: ${e.url}\nMODE: ${e.mode}\nCONTENT:\n${e.content}`).join("\n\n---\n\n");}
function allowed(rows){return rows.map(x=>x.url);}
function selectedUrls(raw,rows){const map=new Map(rows.map(x=>[safeUrl(x.url),x.url])),out=[];for(const u of urlsIn(raw)){const hit=map.get(safeUrl(u));if(hit&&!out.includes(hit))out.push(hit);}return out;}
function score(raw,label){const m=String(raw||"").match(new RegExp(`${label}\\s*:\\s*(10(?:\\.0+)?|[0-9](?:\\.[0-9]+)?)`,`i`));return m?Math.max(0,Math.min(10,Number(m[1]))):0;}
function issues(raw){const m=String(raw||"").match(/ISSUES\s*:\s*([\s\S]*)/i);return text(m?.[1]).slice(0,1600);}
function factCount(raw){const m=String(raw||"").match(/^\s*VERIFIED_FACTS\s*:\s*([\s\S]*?)(?=^\s*[A-Z][A-Z_ ]+\s*:|$)/im);return m?(m[1].match(/^\s*[-•]\s+/gm)||[]).length:0;}
function validPacket(raw){const s=String(raw||"");return /^\s*RESEARCH_PASS\b/im.test(s)&&/^\s*TOPIC\s*:/im.test(s)&&/^\s*PUBLIC_HEADLINE\s*:/im.test(s)&&/^\s*RECENCY\s*:/im.test(s)&&/^\s*VERIFIED_FACTS\s*:/im.test(s)&&/^\s*WHY_IT_MATTERS\s*:/im.test(s)&&/^\s*ANGLE\s*:/im.test(s)&&/^\s*VISUAL_FACTS\s*:/im.test(s)&&/^\s*SOURCES\s*:/im.test(s);}
async function pickTopic(env,profile,rows,baseReply){const system=["You are KAI News Assignment Editor.","Select ONE newest consequential technology development that can support a premium TechVerse post.","Use only rendered evidence. Prefer primary/official sources and events within the last 30 days.","Return exactly TOPIC_QUERY: <short searchable topic>; PRIMARY_URL: <exact URL from allowed list>; WHY: <one sentence>."].join("\n");const user=[`CURRENT_DATE: ${today()}`,`CHANNEL: ${JSON.stringify(profile)}`,baseReply?`CORE RESEARCH HINT:\n${baseReply}`:"",`ALLOWED URLS:\n${allowed(rows).join("\n")}`,`EVIDENCE:\n${bundle(rows)}`].filter(Boolean).join("\n\n");return resilient(env,DIRECTOR_MODEL,CRITIC_MODEL,system,user,600,.12);}
function topicQuery(raw){const m=String(raw||"").match(/^\s*TOPIC_QUERY\s*:\s*(.+)$/im);return text(m?.[1]).slice(0,180);}
async function bingNewsUrls(query){if(!query)return[];const rss=`https://www.bing.com/news/search?q=${encodeURIComponent(query)}&format=rss`;try{const r=await directFetch(rss,8000);if(!r.ok)return[];const xml=await r.text();const raw=[];for(const m of xml.matchAll(/<item>[\s\S]*?<link>([\s\S]*?)<\/link>[\s\S]*?<\/item>/gi))raw.push(htmlDecode(m[1].replace(/<!\[CDATA\[|\]\]>/g,"").trim()));const resolved=[];for(const u of uniq(raw).slice(0,8)){try{const x=await directFetch(u,7000);const final=safeUrl(x.url);if(final&&!/\.bing\.com$|^bing\.com$/.test(new URL(final).hostname))resolved.push(final);}catch{if(safeUrl(u))resolved.push(u);}}return uniq(resolved).slice(0,8);}catch{return[];}}
async function acquire(env,profile,baseReply){const existing=urlsIn(baseReply);let candidates=[...existing];if(isTechverse(profile)){const hubResults=await Promise.all(TECH_HUBS.map(h=>linksFromHub(env,h)));candidates.push(...hubResults.flat());candidates.push(...TECH_HUBS);}else candidates.push(...existing);
  const first=await renderMany(env,candidates,10);if(first.length<2)throw new Error(`insufficient rendered evidence (${first.length} usable sources after browser + direct-fetch acquisition)`);
  if(!isTechverse(profile))return first;
  const choice=await pickTopic(env,profile,first,baseReply);const q=topicQuery(choice);const news=await bingNewsUrls(q);const second=await renderMany(env,news,6);const merged=[];const seen=new Set();for(const row of [...first,...second]){const u=safeUrl(row.url);if(!u||seen.has(u))continue;seen.add(u);merged.push(row);}return merged.slice(0,14);
}
async function synthesize(env,profile,baseReply,rows,note=""){const tech=isTechverse(profile);const system=["You are KAI Research Director for ACC Studio Content.",tech?"TECHVERSE: Latest First. Choose the newest consequential supported technology development; stale evergreen topics are unacceptable.":"Prefer current consequential source-grounded information.","Use ONLY facts in RENDERED EVIDENCE. Never add facts from memory.","Prefer primary/official sources. Separate fact from interpretation.","Every output URL must be copied exactly from ALLOWED URLS.","Return exactly: RESEARCH_PASS; TOPIC; PUBLIC_HEADLINE; RECENCY; VERIFIED_FACTS with at least four bullets; CONTEXT; WHY_IT_MATTERS; COUNTERPOINT_OR_RISK; ANGLE; KEY_POINTS; VISUAL_FACTS; RISK_NOTES; SOURCE_NOTES with GROUNDING_URLS containing at least two exact URLs on one line; SOURCES with exact URLs."].join("\n");const user=[`CURRENT_DATE: ${today()}`,`CHANNEL:\n${JSON.stringify(profile)}`,baseReply?`CORE RESEARCH HINT (not authoritative):\n${baseReply}`:"",`ALLOWED URLS:\n${allowed(rows).join("\n")}`,`RENDERED EVIDENCE:\n${bundle(rows)}`,note,"Build a newsroom-grade evidence packet without inventing anything."].filter(Boolean).join("\n\n");return resilient(env,DIRECTOR_MODEL,CRITIC_MODEL,system,user,3000,.24);}
async function audit(env,profile,candidate,rows){const system=["You are an independent Research Standards Board.","Judge only against rendered evidence.","Return RECENCY_SCORE, SOURCE_AUTHORITY_SCORE, EVIDENCE_SCORE, SPECIFICITY_SCORE, EDITORIAL_VALUE_SCORE, OVERALL_RESEARCH_SCORE (0-10), then ISSUES.","Unsupported claims cap overall below 6. Stale/generic TechVerse topics cap overall below 8. Two genuinely usable exact sources and at least four supported facts are mandatory."].join("\n");return resilient(env,CRITIC_MODEL,DIRECTOR_MODEL,system,[`CURRENT_DATE: ${today()}`,`CHANNEL: ${profile.name}`,`EVIDENCE:\n${bundle(rows)}`,`CANDIDATE:\n${candidate}`].join("\n\n"),1200,.05);}
async function buildResearch(env,profile,baseReply){const rows=await acquire(env,profile,baseReply);let candidate=await synthesize(env,profile,baseReply,rows);let board=await audit(env,profile,candidate,rows);let overall=score(board,"OVERALL_RESEARCH_SCORE"),passes=1;let sources=selectedUrls(candidate,rows);if(overall<MIN_RESEARCH||!validPacket(candidate)||sources.length<2||factCount(candidate)<4){candidate=await synthesize(env,profile,baseReply,rows,`BOARD REJECTION:\n${issues(board)}\nRebuild from evidence. Replace weak/stale angle rather than polishing it.`);board=await audit(env,profile,candidate,rows);overall=score(board,"OVERALL_RESEARCH_SCORE");passes=2;sources=selectedUrls(candidate,rows);}if(!validPacket(candidate)||sources.length<2||factCount(candidate)<4||overall<MIN_RESEARCH)throw new Error(`research quality below studio threshold (${overall.toFixed(1)}/10; rendered ${rows.length}; sources ${sources.length}; facts ${factCount(candidate)})`);return{reply:candidate,meta:{revision:REVISION,role:"KAI_RESEARCH_DIRECTOR",researchScore:overall,threshold:MIN_RESEARCH,boardPasses:passes,renderedEvidenceCount:rows.length,sourceCount:sources.length,factCount:factCount(candidate),recencyScore:score(board,"RECENCY_SCORE"),sourceAuthorityScore:score(board,"SOURCE_AUTHORITY_SCORE"),evidenceScore:score(board,"EVIDENCE_SCORE"),specificityScore:score(board,"SPECIFICITY_SCORE"),editorialValueScore:score(board,"EDITORIAL_VALUE_SCORE"),acquisitionModes:Array.from(new Set(rows.map(x=>x.mode))),criticIssues:issues(board)}};}
async function health(request,env,ctx){const up=await baseWorker.fetch(request,env,ctx);let d={};try{d=await up.clone().json();}catch{}return json({...(d&&typeof d==="object"?d:{}),kaiResearchIntelligence:"ACTIVE",kaiResearchRevision:REVISION,kaiResearchAcquisition:"OFFICIAL_HUBS_PLUS_TARGETED_NEWS_PLUS_BROWSER_AND_FETCH_FALLBACK",kaiResearchThreshold:MIN_RESEARCH},up.status,up.headers);}
export default{async fetch(request,env,ctx){const url=new URL(request.url);if(request.method==="GET"&&(url.pathname==="/health"||url.pathname==="/api/acc-ai"))return health(request,env,ctx);if(!(request.method==="POST"&&url.pathname==="/api/acc-ai"))return baseWorker.fetch(request,env,ctx);let body;try{body=await request.clone().json();}catch{return baseWorker.fetch(request,env,ctx);}if(stageOf(body)!=="RESEARCH")return baseWorker.fetch(request,env,ctx);const upstream=await baseWorker.fetch(request,env,ctx);let payload={};try{payload=await upstream.clone().json();}catch{}const baseReply=text(payload?.reply);try{const built=await buildResearch(env,profileOf(body),baseReply);return json({...payload,ok:true,reply:built.reply,provider:`${text(payload?.provider)||"ACC OS X"} + KAI Research 255.1`,kaiResearch:built.meta},200,upstream.headers);}catch(error){return json({ok:false,stage:"RESEARCH",status:"KAI_RESEARCH_QUALITY_BLOCKED",error:`KAI_RESEARCH_QUALITY_BLOCKED: ${String(error?.message||error)}`,errorDetail:{code:"KAI_RESEARCH_QUALITY_BLOCKED",revision:REVISION,message:String(error?.message||error)}},422,upstream.headers);}}};
