ACC OS X — GM3 FULL PACKAGE / PWA X LOGO FIX

Base: GM2 successful root deployment.
Change scope: PWA/app icon uses ACC OS X X-logo assets (no checkmark).
Keep all files in repository root when uploading/deploying.
GM2 should be retained as recovery checkpoint until GM3 is verified live.
